'use strict';

/**
 * neighborhood-connect-event service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::neighborhood-connect-event.neighborhood-connect-event');
