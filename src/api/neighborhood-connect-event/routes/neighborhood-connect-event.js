'use strict';

/**
 * neighborhood-connect-event router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::neighborhood-connect-event.neighborhood-connect-event');
